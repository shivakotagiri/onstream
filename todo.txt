/validate-email.ts 
check email format, ---> validate.js
prevent disposable emails, ---> mailchecker
check the email in MX records, ---> deep-email-vaildator or node:dns/promises
create the unverified user, 
send mail verifcation, 
delete unverified user after 24hrs,
--------------------------------------------------------------------------------

api based tools for above ----> kickbox api, mailboxvalidator, varifalia

--------------------------------------------------------------------------------
/ratelimiting --> upstash/redis upstash/ratelimit

Account lockout after repeated failed logins

session hardening (short expiry + rotation + secure cookies)
--------------------------------------------------------------------------------
Level 2 — Easy
(small logic, no infra)

    Middleware guard
        Allow only POST on auth routes
        Block missing headers / bad user-agents

    Account lockout
        Lock user after N failed login attempts
        Add cooldown (e.g. 15–30 mins)
--------------------------------------------------------------------------------
Level 3 — Medium (one external service)
    Edge rate limiting
        Upstash @upstash/ratelimit
        Apply in middleware.ts to /api/auth/*

    Bot protection
        Cloudflare Turnstile on login/signup/reset
--------------------------------------------------------------------------------



Current TODOs to work with:- 
    - issue:- fix delay in signout from all the devices
        explanation:-
            when the user clicks the "sign out everywhere" -> it actually revokes all the devices session but the problem is 
            -> since we cached the session cookies for 5mins, the devices cant be signed out for 5 mins eventhough the sessions are revoked in the DB

            -> if we remove the cache the DB hits increases per each user request
            -> if we ensure the cache, the user from different devices cant be signed out
            
            -> SOLUTION:- https://chatgpt.com/c/6999e60a-8d8c-8320-ad74-151157ec9c21
                Session Version Pattern (try to search about it in google)

    - add the phoneNumber (through verifcation via OTP)
    - there is a bug in ShowBlowckedUsersDialog.tsx find it later
    - bug in email confirmation and verifcation & check whether the new email is available or not before assigning it
    - add a ban feature
    - here there is cache in profile which is making it an issue after saving the account info from /welcome page
    - whenever the user follows a person, the followers count doesn't refresh
    - whenever you refresh the page the follow-button state restarts(from default value to actuall value)

-----------------------------------------------------------------------------------------------
    
Performance issues

Fix 5 — DB call explosion (real reason most devs miss)

Your page does:

userSearchData

getCurrentUser

isCurrentUserFollowing

isUserBlocked

isUserBlocked

userFollowers

usersFollowed

👉 That is 7 DB calls on first render.

Production apps combine these.

⭐ Senior Level Fix (VERY IMPORTANT)

Create one action:

👉 getUserProfilePageData()

Fetch everything in ONE DB query (joins).

This is how Twitter / Instagram do.

🚀 Extra performance tips for your code
1. Image
quality={100} ❌ very expensive


Use:

quality={75}

2. Avoid router.refresh after follow

That triggers full server rerender.

Better:

optimistic update (you already do)

no refresh unless needed

Huge UX improvement.